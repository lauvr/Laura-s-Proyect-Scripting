using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Feature04 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Feature 4 :D ");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
