using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class feature02 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("feature02 :D");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
